// Using Two extra array

/*

1. Keep all non-zero elements in one array.

2. And zero elements in another array

3. At last Just merge both array. (non-zero elements array) + (zero elements array)

*/
